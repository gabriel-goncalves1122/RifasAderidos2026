rules_version = '2';

// Permite leitura e escrita no Storage para qualquer usuário que tenha feito login no sistema
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}